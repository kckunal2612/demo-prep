description = "Pick specific features or fixes to include in your demo deck"
prompt = "Run demo-prep in pick mode. Gather what was shipped, show a numbered list grouped by features / fixes / infra in plain English, ask which items to include (numbers or 'all') and what technical depth (high / medium / low), then generate the deck with only the selected items."
